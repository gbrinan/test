# 다국어(i18n) 준비 문서

> 상태: **기반만 구축** (2026-07-21). 한국어가 기본이며, 다른 언어 추가 시 이 문서의 순서대로 켜면 된다.

## 현재 준비된 것

| 항목 | 위치 | 상태 |
|---|---|---|
| UI 문자열 사전 (한국어) | `i18n/ko.json` | ✅ 주요 UI 문자열 55키 추출 완료 |
| UI 문자열 사전 (영어) | `i18n/en.json` | ✅ UI 번역 완료 (챕터 콘텐츠 제외) |
| 로더 + `t()` 헬퍼 | `claude-guide-demo.html` 스크립트 하단 | ✅ 언어 감지(`?lang=` → localStorage → ko), 사전 로드 |
| 문자열 치환 배선 | — | ⏸ 미적용 (아래 2단계) |
| 챕터 콘텐츠 번역 | — | ⏸ 미착수 (아래 3단계) |

## 활성화 로드맵

### 1단계 — 완료: 사전·로더
- 키 규칙: `영역.이름` (예: `paywall.title`). 변수는 `{tier}`, `{lv}`, `{pct}`, `{name}` 플레이스홀더.
- 언어 선택: URL `?lang=en` 또는 localStorage `cg-lang`. 기본 `ko`.

### 2단계 — UI 치환 배선 (반나절 작업)
- 코드의 하드코딩 한국어를 `t("key","한국어 원문")` 호출로 교체. 원문을 fallback으로 남기므로 사전 누락 시에도 깨지지 않음.
- `TIERS[].name` → `t("tier.challenger")` 등으로 교체 (진화 문구·결제 주문명까지 자동 전파됨).
- 헤더에 언어 선택 UI(예: 🌐 셀렉트) 추가.

### 3단계 — 챕터 콘텐츠 (본 작업)
- `CHAPTERS` 배열(33챕터, 본문 평균 ~3,000자)을 `content/ko.json`으로 추출하고, 언어별 `content/<lang>.json`을 로드하는 구조로 전환.
- 번역 시 주의:
  - **퀴즈 정답 인덱스(`a`)는 번역 후에도 보기 순서와 일치**해야 함 — 번역 후 자동 검증 스크립트(기존 node 검증 재사용) 필수.
  - 비유 카드(인천공항·미슐랭 식당·매트릭스 등)는 직역보다 **현지 등가물 치환** 검토 (예: 배달의민족 → DoorDash).
  - mermaid 노드 라벨도 콘텐츠에 포함 — 함께 번역.
  - 한국 특화 내용(HWPX, 식약처, 표시광고법, 부산대 맞춤법)은 대상 시장에 맞게 각색 또는 "한국 시장 사례" 명시.
- 케이스 볼트의 실제 기업 사례(아뜨랑스·미니웰 등)는 번역하되 수치·고유명사 유지.

### 4단계 — 주변 요소
- 결제: PortOne은 국내 PG — 해외 결제는 Stripe 등 별도 어댑터 필요 (`access-config`의 paymentProvider 확장).
- 폰트: Galmuri는 한글 픽셀 폰트지만 라틴 글리프 포함 — 영어 UI는 그대로 사용 가능. 일본어·중국어는 별도 픽셀 폰트 필요.
- 이메일 템플릿(Supabase Auth) 언어별 분리.
- BGM/효과음은 언어 무관 — 유지.

## 새 언어 추가 절차 (요약)

1. `i18n/<lang>.json` 생성 — `ko.json` 키를 복사해 번역 (`_meta.status` 갱신)
2. (3단계 완료 후) `content/<lang>.json` 번역 + 퀴즈 검증 스크립트 통과
3. 언어 선택 UI에 옵션 추가 → 끝
